import 'package:flutter/cupertino.dart';

class MenuItem {
  final IconData icon;
  final String title;
  final String color;
  final String routeName;
  const MenuItem(this.icon, this.title, this.color, this.routeName);
}
